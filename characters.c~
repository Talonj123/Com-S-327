 
#include "characters.h"

monster_t  get_new_monster()
{
  monster_t monster;
  char attributes = rand()%16;
  monster.attributes.raw = attributes;
  if (attributes < 0xA)
  {
    monster.symbol = '0' + attributes;
  } else
  {
    monster.symbol = 'A' + attributes - 0xA;
  }
  return monster;
}
